<p id="notice"><%= notice %></p>

<div class="post">
        <span class="left"><%= get_user(@post.user_id)  %><br />
          <b>Created: </b><%=@post.created_at %><br />
          <b>Updated: </b><%= @post.updated_at %><br />
          <b>Votes: </b><%=  @post.votes.count %><br />

        </span>
  <span class="right"><%= @post.content %></span>
        <span class="bottom"><%= link_to "Edit", edit_post_path(@post) %> <br />
          <%= link_to 'Delete', @post, method: :delete, data: { confirm: 'Are you sure?' } %> <br />
          <%= link_to 'Vote', @post, method: :post %>   <br />

        </span>
</div>
<h3>Comments</h3>
  <% Post.all.each do |p| %>
           <% if (@post.id == p.parent_id) %>
          <div class="post" >
        <span class="comment-left"><%= get_user(p.user_id) %><br />
          <b>Created: </b><%=p.created_at %><br />
          <b>Updated: </b><%= p.updated_at %><br />
          <b>Votes: </b><%=  %>

        </span>
            <span class="comment-right"><%= p.content %></span>
        <span class="comment-bottom"><%= link_to "Edit", edit_post_path(p) %> <br />
          <%= link_to 'Delete', p, method: :delete, data: { confirm: 'Are you sure?' } %> <br />
          <%= link_to 'Vote', p, method: :post %>   <br />

        </span>
          </div>


  <% end %>

<%end %>
                            <br />
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
